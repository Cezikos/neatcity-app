import { StyleSheet } from 'react-native';

import COLORS from '../../styles/colors';

export default StyleSheet.create({
  headline: {
    alignSelf: 'center',
    color: COLORS.BLACK,
    fontSize: 20
  }
});
